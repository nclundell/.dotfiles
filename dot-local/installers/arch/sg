#! /usr/bin/env bash

# Ensure the sg kernel module is loaded at boot
echo "Enabling sg kernel module for SCSI generic support..."
echo sg | sudo tee /etc/modules-load.d/sg.conf > /dev/null

# Load the sg module immediately
echo "Loading sg kernel module..."
sudo modprobe sg
