#!/bin/sh

configs=()

for config in "$@" 
do
  case "${config}" in
    "all")
      configs+=("abcde" "alacritty" "nvim")
      ;;
    "personal")
      configs+=("abcde" "alacritty" "nvim")
      ;;
    "work")
      configs+=("alacritty" "nvim")
      ;;
    *)
      configs+=("${config}")
      ;;
  esac
done

# Un-Stow Configs
for config in "${configs[@]}"
do
  echo "UNINSTALLING: ${config}"
  stow -D "${config}"
done
