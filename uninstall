#!/bin/sh

configs=()

for config in "$@" 
do
  case "${config}" in
    "all")
      configs+=("abcde" "nvim")
      break
      ;;
    "personal")
      configs+=("abcde" "nvim")
      break
      ;;
    "work")
      configs+=("nvim")
      break
      ;;
    *)
      configs+=("${config}")
      ;;
  esac
done

# Un-Stow Configs
for config in "${configs[@]}"
do
  echo "UNINSTALLING: ${config}"
  stow -D "${config}"
done
