#!/bin/sh

ASDF = $HOME/.local/share/asdf

# Install or Update ASDF
if [[ -d "$HOME/.local/share/asdf" ]]; then
  cd $HOME/.local/share/asdf
  git pull
else
  git clone https://github.com/asdf-vm/asdf.git ~/.local/share/asdf
fi
